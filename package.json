{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Task Manager App — Intern Assignment",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend"
  },
  "private": true
}
